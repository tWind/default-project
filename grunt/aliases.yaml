default:
  description: 'Default (production) build'
  tasks:
  - sprite
  - copy
  - sass
  - autoprefixer
  - browserify
  - concat
  - imagemin
  - connect
  - watch

dev:
  description: 'dev'
  tasks:
    - watch