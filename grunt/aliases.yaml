default:
  description: 'Default (production) build'
  tasks:
    - sprite
    - copy
    - sass
    - autoprefixer
    - concat
    - connect
    - watch

dev:
  description: 'dev'
  tasks:
    - watch